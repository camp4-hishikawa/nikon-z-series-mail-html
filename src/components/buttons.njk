{% from 'gap.njk' import gap %}
{% from 'button.njk' import button %}
{% macro buttons(bodyId, productUrl, specialUrl, purchaseUrl) %}
{{ button(productUrl, bodyId + '/img/button-product.png', '製品詳細') }}
{{ gap(20) }}
{{ button(specialUrl, bodyId + '/img/button-special.png', 'スペシャルコンテンツ') }}
{{ gap(20) }}
{{ button(purchaseUrl, bodyId + '/img/button-purchase.png', '購入はこちら') }}
{% endmacro %}
